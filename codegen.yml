overwrite: true
# schema: "http://localhost:4000/graphql"
# documents: "./src/graphql/*.graphql"
generates:
  src/generated/graphql.ts:
    schema: ./src/graphql/sample.graphql
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
      # - "typescript-react-apollo"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
